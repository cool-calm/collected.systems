---
// import "../html-rewriter-polyfill";
import "../self-polyfill";
import "@worker-tools/html-rewriter/polyfill-base64"
import { sourceFromGitHubRepo } from "@collected/press-server";

// const source = sourceFromGitHubRepo("RoyalIcing", "RoyalIcing");
const source = sourceFromGitHubRepo("cool-calm", "collected-press");
const url = new URL("https://collected.systems/");
return await source.serveURL(url);
---
